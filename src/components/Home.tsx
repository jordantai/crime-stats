import React from 'react';
import { RouteComponentProps } from '@reach/router';

const Home = (_: RouteComponentProps) => {
  return (
    <div>
      <h2>Welcome</h2>
    </div>
  );
};

export default Home;
